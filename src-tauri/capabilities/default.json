{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the usage overlay",
  "windows": ["main", "edge-tab"],
  "permissions": [
    "core:event:default",
    "notification:default"
  ]
}
