{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Minimum permissions for the draggable settings window",
  "windows": ["settings", "history"],
  "permissions": [
    "core:event:default",
    "core:window:allow-start-dragging",
    "dialog:allow-save"
  ]
}
