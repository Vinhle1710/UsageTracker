import {describe,it,expect} from "vitest";import {resolvePalette,adaptPalette} from "./colors";const c={session:"#111111",weekly:"#222222",api:"#333333",single:"#444444",background:"#000000",text:"#fff"};describe("palette",()=>{it("resolves modes",()=>{expect(resolvePalette("greyscale",c,"dark").session).toBe("#d1d5db");expect(resolvePalette("single-color",c,"light").weekly).toBe(c.single);expect(resolvePalette("multicolor",c,"dark").api).toBe(c.api)});it("adapts text",()=>{expect(adaptPalette(c,"light").text).toBe("#111827");expect(adaptPalette(c,"dark").text).toBe("#f9fafb")})});
