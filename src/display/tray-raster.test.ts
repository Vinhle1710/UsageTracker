import {describe,it,expect} from "vitest";import {rasterizeTrayIndicator} from "./tray-raster";describe("tray raster",()=>it("returns premultiplied-sized RGBA payload",()=>{const image=rasterizeTrayIndicator({provider:"x",style:"compact",metrics:[{id:"session",usedPercent:42,displayPercent:42,label:"42% used",severity:"normal"}]},{session:"#fff"},32);expect(image.width).toBe(32);expect(image.height).toBe(32);expect(image.rgba).toHaveLength(32*32*4)}));
