import {useState} from "react";import {invoke} from "@tauri-apps/api/core";export function ExportControls(){const [confirm,setConfirm]=useState(false);const [message,setMessage]=useState("");return <div><button onClick={()=>setMessage("Choose a destination in the export flow.")}>Export</button><button onClick={()=>setConfirm(true)}>Clear history</button>{confirm&&<span><button onClick={()=>{void invoke("clear_history").then(()=>{setMessage("History cleared.");setConfirm(false)}).catch(()=>setMessage("Could not clear history."))}}>Confirm clear</button><button onClick={()=>setConfirm(false)}>Cancel</button></span>}<span role="status">{message}</span></div>}
