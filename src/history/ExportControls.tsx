import {useEffect,useRef,useState} from "react";import {chooseHistoryExportPath,clearHistory,exportHistory} from "./api";import type {HistoryQuery} from "./types";
const defaultQuery:HistoryQuery={from:0,to:Math.floor(Date.now()/1000)+1,provider:null,windowKind:null};
export function ExportControls({query=defaultQuery,onCleared}:{query?:HistoryQuery;onCleared?:()=>void}){const [format,setFormat]=useState<"json"|"csv">("json"),[confirm,setConfirm]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState("");const clearRef=useRef<HTMLButtonElement>(null),confirmRef=useRef<HTMLButtonElement>(null);useEffect(()=>{(confirm?confirmRef:clearRef).current?.focus()},[confirm]);useEffect(()=>{if(!confirm)return;const f=(e:KeyboardEvent)=>{if(e.key==="Escape")setConfirm(false)};window.addEventListener("keydown",f);return()=>window.removeEventListener("keydown",f)},[confirm]);const doExport=async()=>{setBusy(true);setMessage("");try{const path=await chooseHistoryExportPath(format);if(!path){setMessage("Export cancelled.");return}await exportHistory(query,format,path);setMessage("Export complete.")}catch(e){setMessage(`Export failed: ${String(e)}`)}finally{setBusy(false)}};const doClear=async()=>{setBusy(true);try{await clearHistory();setConfirm(false);setMessage("History cleared.");onCleared?.()}catch(e){setMessage(`Could not clear history: ${String(e)}`)}finally{setBusy(false)}};return <section aria-label="History export and clearing"><h2>Export and clear</h2><label>Format <select value={format} onChange={e=>setFormat(e.target.value as "json"|"csv")}><option value="json">JSON</option><option value="csv">CSV</option></select></label><button type="button" onClick={()=>void doExport()} disabled={busy}>Export history</button><button ref={clearRef} type="button" onClick={()=>setConfirm(true)} disabled={busy}>Clear history</button>{busy&&<p role="status">Working…</p>}{message&&<p role="status">{message}</p>}{confirm&&<div role="dialog" aria-modal="true" aria-labelledby="clear-title"><h3 id="clear-title">Confirm clear history</h3><p>This permanently removes local history.</p><button ref={confirmRef} type="button" onClick={()=>void doClear()} disabled={busy}>Confirm clear</button><button type="button" onClick={()=>setConfirm(false)} disabled={busy}>Cancel</button></div>}</section>}
