import {fireEvent,render,screen,waitFor} from "@testing-library/react";import {describe,expect,it,vi,beforeEach,afterEach} from "vitest";import {HistoryApp} from "./HistoryApp";import {invoke} from "@tauri-apps/api/core";vi.mock("@tauri-apps/api/core",()=>({invoke:vi.fn()}));const p={provider:"claude",windowKind:"session_5h",sampledAt:1,usedPercent:25,model:null,apiCalls:null,estimatedCostMicros:null,overageCostMicros:null};describe("HistoryApp",()=>{beforeEach(()=>vi.mocked(invoke).mockReset());afterEach(()=>document.body.innerHTML="");it("summarizes and reports unavailable metrics",async()=>{vi.mocked(invoke).mockResolvedValue({points:[p],billing:[]});render(<HistoryApp now={()=>3000000}/>);await waitFor(()=>expect(screen.getByText(/Session usage/)).toBeTruthy());expect(screen.getByText(/Per-model data unavailable/)).toBeTruthy()});it("reloads filters and range",async()=>{vi.mocked(invoke).mockResolvedValue({points:[],billing:[]});render(<HistoryApp now={()=>3000000}/>);fireEvent.click(screen.getAllByRole("button",{name:"30 days"})[0]);fireEvent.change(screen.getByLabelText(/Provider/),{target:{value:"claude"}});await waitFor(()=>expect(invoke).toHaveBeenCalledWith("query_history",expect.objectContaining({query:expect.objectContaining({provider:"claude",from:408000})})))})});
