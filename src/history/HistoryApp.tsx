import {useEffect,useState} from "react";import {invoke} from "@tauri-apps/api/core";import {historyBounds} from "./range";import type {HistoryRange,HistoryResult} from "./types";import {HistoryChart} from "./HistoryChart";import {BillingTable} from "./BillingTable";import {ExportControls} from "./ExportControls";let requestId=0;
export function HistoryApp({now=()=>Math.floor(Date.now()/1000)}:{now?:()=>number}){const [range,setRange]=useState<HistoryRange>("24h"),[provider,setProvider]=useState(""),[series,setSeries]=useState(""),[result,setResult]=useState<HistoryResult>({points:[],billing:[]}),[error,setError]=useState(""),[loading,setLoading]=useState(false);useEffect(()=>{const id=++requestId;setLoading(true);const b=historyBounds(range,now());void invoke<HistoryResult>("query_history",{query:{...b,provider:provider||null,windowKind:series||null}}).then(v=>{if(id===requestId)setResult(v)}).catch(e=>{if(id===requestId)setError(String(e))}).finally(()=>{if(id===requestId)setLoading(false)})},[range,provider,series]);return <main><h1>History</h1><nav aria-label="History range">{(["5h","24h","7d","30d"] as HistoryRange[]).map(v=><button key={v} onClick={()=>setRange(v)} aria-pressed={range===v}>{v}</button>)}</nav><label>Provider<select value={provider} onChange={e=>setProvider(e.target.value)}><option value="">All</option><option value="claude">Claude</option><option value="openai">OpenAI</option></select></label><label>Series<select value={series} onChange={e=>setSeries(e.target.value)}><option value="">All</option>{Array.from(new Set(result.points.map(p=>p.windowKind))).map(v=><option key={v}>{v}</option>)}</select></label><HistoryChart points={result.points} selectedSeries={series} loading={loading} error={error}/><BillingTable entries={result.billing}/><ExportControls/></main>}
