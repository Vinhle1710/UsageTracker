import {useMemo,useRef,useState} from "react";import type {HistoryPoint} from "./types";
export function HistoryChart({points,selectedSeries,loading,error}:{points:HistoryPoint[];selectedSeries?:string;loading?:boolean;error?:string}){const [active,setActive]=useState<HistoryPoint|null>(null);const refs=useRef<Record<string,HTMLButtonElement|null>>({});const series=useMemo(()=>selectedSeries?points.filter(p=>p.windowKind===selectedSeries):points,[points,selectedSeries]);if(loading)return <p role="status">Loading usage history…</p>;if(error)return <p role="alert">{error}</p>;if(!series.length)return <p role="status">No usage samples in this range.</p>;const min=Math.min(...series.map(p=>p.sampledAt)),max=Math.max(...series.map(p=>p.sampledAt),min+1),groups=Array.from(new Set(series.map(p=>`${p.provider}/${p.windowKind}`))),pos=(p:HistoryPoint)=>({x:(p.sampledAt-min)/(max-min)*600+20,y:220-p.usedPercent*2});return <section aria-label="Usage history visualization"><div style={{position:"relative"}}><svg role="img" aria-label="5 hour usage history chart" viewBox="0 0 640 240"><title>Usage history chart</title><desc>{series.map(p=>`${p.provider} ${p.windowKind}: ${p.usedPercent}% at ${new Date(p.sampledAt*1000).toISOString()}`).join("; ")}</desc>{groups.map((group,g)=>{const ps=series.filter(p=>`${p.provider}/${p.windowKind}`===group);return <g key={group} data-series={group}><polyline points={ps.map(p=>{const q=pos(p);return `${q.x},${q.y}`}).join(" ")} fill="none" stroke={`var(--history-series-${g%4+1})`}/>{ps.map(p=>{const q=pos(p);return <circle key={`${group}-${p.sampledAt}`} cx={q.x} cy={q.y} r="5" aria-hidden="true"/>})}</g>})}</svg>{groups.flatMap(group=>series.filter(p=>`${p.provider}/${p.windowKind}`===group).map((p,i,ps)=>{const q=pos(p),key=`${group}-${p.sampledAt}`;return <button key={key} ref={n=>{refs.current[key]=n}} type="button" style={{position:"absolute",left:`${q.x/640*100}%`,top:`${q.y/240*100}%`,width:24,height:24,opacity:0}} aria-label={`${p.provider} ${p.windowKind} ${p.usedPercent}%`} onFocus={()=>setActive(p)} onKeyDown={e=>{if(e.key!=="ArrowLeft"&&e.key!=="ArrowRight")return;e.preventDefault();const n=ps[Math.max(0,Math.min(ps.length-1,i+(e.key==="ArrowRight"?1:-1)))];refs.current[`${group}-${n.sampledAt}`]?.focus();setActive(n)}}/>}))}</div><p role="status">{active?`${active.provider}, ${active.usedPercent}% (${active.windowKind})`:"Select a chart point to inspect usage."}</p><p className="sr-only">{series.map(p=>`${p.provider} ${p.windowKind}: ${p.usedPercent}% at ${new Date(p.sampledAt*1000).toISOString()}`).join("; ")}</p></section>}
